<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>No.</th>
      <th>Name</th>
      <th>Email</th>
      <th>Status</th>

      <th>Username</th>
      <th>Aksi</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i = 0; i < user.length; i++){ %>
    <tr>
      <td><%= i + 1 %></td>
      <td><%= user[i].name %></td>
      <td><%= user[i].email %></td>
      <td><%= user[i].username %></td>
      <td>
        <input data-id="<%= user[i].id %>" class="toggle-class" type="checkbox" data-onstyle="success" data-offstyle="danger" data-toggle="toggle" data-on="Active" data-off="InActive" <%= user[i].status ? 'checked' : '' %>>
      </td>
      <td>
        <form
          action="/user/delete/<%= user[i]._id %>?_method=DELETE"
          method="POST"
        >
          <a
            href="/user/edit/<%= user[i]._id %>"
            class="btn btn-info btn-sm"
          >
            <i class="fas fa-pencil-alt"></i>
            Ubah
          </a>
          <button class="btn btn-danger btn-sm">
            <i class="fas fa-trash"></i>
            Hapus
          </button>
        </form>
      </td>
    </tr>
    <% } %>
  </tbody>
</table>
